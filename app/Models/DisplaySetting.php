<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class DisplaySetting extends Model
{
    protected $fillable = ['textup', 'textdown', 'video', 'bgimg', 'work', 'deptflag', 'doctorflag'];

   
}
